<!DOCTYPE html>
<html>
<center><h1>Kan Ban Cooking</h1>
<h5>Select and move objects to create a burger!</h5></center>
<head>
  <meta charset="utf-8">
  <title>Recipe</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.min.css">
  <link rel="stylesheet" href="/styles.css">
</head>

<body>

  <div class="container">

    <div class="row">
      {{#each steps}}
      <div class="column step">

        <div class="column ">
          <div class="header">
          <p>{{name}}</p>
          </div>
          <form class="" action="/recipe/move" method="post">
            <select name="recipe_id" class="">
            {{#each recipes}}
              <option class="item" value="{{id}}">{{recipe_name}}</option>
            {{/each}}
            <input type="submit" name="" value="Move">
              </select>
          </form>
          {{#each recipes}}
          <div class="item">
            <img src="{{img_url}}" alt="{{recipe_name}}">
          <p>{{recipe_name}}</p>
          </div>
          {{/each}}
        </div>

      </div>
      {{/each}}
    </div>
  </div>




  </div>

</body>

</html>
